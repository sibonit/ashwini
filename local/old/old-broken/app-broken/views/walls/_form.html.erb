<%= form_for(@new_wall) do |f| %>

  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <p>&nbsp;</p>
  
  <%= f.label :description %>
  <%= f.text_area :description, :cols => 72, :rows => 5 %>
  
  <%= f.submit "Create New Wall" %>

<% end %>